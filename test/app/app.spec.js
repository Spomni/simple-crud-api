describe('app', () => {
  
  describe('createApp', () => {
    it.todo('should setup router module to use app error handler by default')
    it.todo('should return a router instance')
    it.todo('should create returned instance later than router error handler was setup')
    it.todo(`should register personControler at the path "/person"`)
  })
})