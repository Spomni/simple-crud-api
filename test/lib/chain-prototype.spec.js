describe('chain-prototype', () => {

  describe('chainPrototype()', () => {
    
    it('should return object passed as child option')
    it('should add parent as child prototype')
    it('should not lose original prototype')
    it('should add parent as prototype of original proto with number shifting plus one')
    it('should not lose original prototype chain')
    it('should add parent the last prototype in chain if shifting options is greater than cain length')
  })
})